# PLAID-to-IDOT
Portable pipeline converting PLAID layout CSVs into Dispendix iDOT Assay Studio import files. Uses config.json, merges compound metadata, selects feasible stock dilutions, computes dispense volumes, enforces strict DMSO cap + normalization, and outputs protocol + liquids map with traceable naming.
